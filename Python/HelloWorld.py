#!/usr/bin/python3.7

# R-pi; locate the correct Python version using locate python
# Use only lf (\n) as line-endings, no crlf allowed
# After upload, CHMOD to a minimum of 744 (allow execution for user)

print("Hello world!")
